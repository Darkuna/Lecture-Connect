import {AvailabilityMatrix} from '../availability-matrix';

describe('AvailabilityMatrix', () => {
  it('should create an instance', () => {
    expect(new AvailabilityMatrix([])).toBeTruthy();
  });
});
